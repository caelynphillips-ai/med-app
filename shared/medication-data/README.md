# Medication Data

`medications.json` is the normalized local medication suggestion seed used by the clients.

The source can be refreshed from RxTerms with `scripts/import-rxterms.mjs`. Keep the shape compatible with `shared/schema/medication-suggestion.schema.json`.
